<?php

$functions = [
    "serve" => serve(),
];

if (!in_array($argv[1], $functions, true)) return print "\nNon-existent function.\n\n";

$functions[$argv[1]];

function serve() {
    exec("php -S 127.0.0.1:8080");
}